
const importFile = (file) => {


};

const parser = (file) => {
    const parsed = {
        testCount
        paintColorCount
        costumerCount
        colorMap
    };
};

export default parser;
